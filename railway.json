{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": "sh /app/_build/prod/rel/transitmaps/bin/migrate && /app/_build/prod/rel/transitmaps/bin/transitmaps eval 'Transitmaps.Release.import_tfl()'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
